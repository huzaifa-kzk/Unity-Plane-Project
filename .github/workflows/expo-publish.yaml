name: Expo EAS Update CI/CD

on:
  push:
    branches:
      - main

jobs:
  eas-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: plane-controller-app/package-lock.json

      - name: Clean install dependencies
        working-directory: plane-controller-app
        run: |
          rm -rf node_modules package-lock.json
          npm install
          npm install -g eas-cli@latest
          npm install eas-cli@latest --save-dev

      - name: Check and fix image files
        working-directory: plane-controller-app
        run: |
          sudo apt-get update
          sudo apt-get install -y file imagemagick

          if [ -d "assets" ]; then
            find assets -type f \( -name "*.png" -o -name "*.jpg" -o -name "*.jpeg" \) | while read img; do
              if [ ! -s "$img" ]; then
                convert -size 10x10 xc:#cccccc "$img" 2>/dev/null || true
              fi

              if ! identify "$img" 2>/dev/null; then
                convert -size 10x10 xc:#cccccc "$img" 2>/dev/null || true
              fi
            done
          fi

      - name: Clear cache
        working-directory: plane-controller-app
        run: |
          rm -rf node_modules/.cache
          rm -rf .expo
          rm -rf dist
          rm -rf .metro

      - name: Run EAS Update
        working-directory: plane-controller-app
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          CI: true
        run: |
          eas update --branch main --message "CI/CD update from GitHub Actions"

  websocket-test:
    runs-on: ubuntu-latest
    needs: eas-update

    steps:
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install WebSocket tester
        run: npm install -g wscat

      - name: Test EC2 WebSocket Connection
        run: |
          echo "Testing WebSocket connectivity..."
          timeout 15 wscat -c ws://YOUR_EC2_PUBLIC_IP:8080 || exit 1
